import React, { useState } from "react";

const Dashboard = () => {
  const [requests, setRequests] = useState([]);
  return (
    <div>
      <h1>Requests</h1>
    </div>
  );
};

export default Dashboard;
